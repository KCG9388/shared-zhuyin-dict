# templates — 各分類字庫模板

這裡每個 `.txt` 都是一份微軟注音的造詞字庫,依分類存放(遊戲、科技、學術、日常、宗教…)。

- **一般使用者不必手動下載**:用「字庫管家」App 的「雲端模板」分頁,挑一個就會自動下載、驗證、引導匯入。
- 檔案格式:UTF-16,每行是「詞 + 兩個空格 + 每個字的注音(含聲調)」。
- `sub_XXX.txt` 是社群投稿、經審核後收錄的模板。

> 哪些模板會出現在 App 清單,是由上一層的 `index.json` 決定,不是直接掃這個資料夾。
